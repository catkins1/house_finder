version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    ports:
    - 27017:27017
    env_file:
      - mongo.env
  mongo-express:
    image: mongo-express
    ports:
      - 8081:8081
    env_file:
      - mongo.env